{
  "name": "mdmaid.desk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Persistent local document inbox and reading workspace for Markdown artifacts",
  "bin": {
    "mdmaid-desk": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/catalog.test.js dist/cli.test.js",
    "check": "npm run test"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "typescript": "^5.8.3"
  }
}
